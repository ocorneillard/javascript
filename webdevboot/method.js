let obj = {
    name: "Dorian",
    age : 24,
    isCool : false,
    friends : ["imaginary Bob", "sister", "old guy"],

    add : (a,b) => {
       c =  a + b
       return c
    }
}

obj.add(10,10)

// Organize your code by adding method in object 

// Avoid namespace collision

// underscore.js ? _.reduceRight() etc

let comments = {}
comments.data = ["Good job!", "Love you !", "Have a nice day"]

